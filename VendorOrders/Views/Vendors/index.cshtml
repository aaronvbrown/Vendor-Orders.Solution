@{
  Layout = "_Layout";
}

@using VendorOrders.Models;

<h1>Customers</h1>

@if (@Model.Count == 0)
{
  <h3> No Customers have been added yet. </h3>
}

<ul>
  @foreach (Vendor vendor in Model)
  {
    @* <li>@Html.ActionLink($"{vendor.Name}", "Details", new {id = vendor.VendorId })</li> *@
    <li><a href='/vendors/@vendor.VendorId'>@vendor.Name</a></li> 
  }
</ul>


<h3><a href='/vendors/new'>Add a new customer</a></h3>

<p><a href='/'>Back home</a></p>